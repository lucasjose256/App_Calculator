/*import 'package:flutter/material.dart';

class Button extends StatelessWidget {
  final String txt;
  final Color btncolor;
  final Color txColor;
  final Function f;
  // ignore: use_key_in_widget_constructors
  const Button(this.txt, this.btncolor, this.txColor, this.f);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: RaisedButton(
        onPressed: () {
          f(txt);
        },
        // ignore: unused_label
        child: Text(
          txt,
          style: TextStyle(color: txColor, fontSize: 35),
        ),

        shape: CircleBorder(),
        color: btncolor,
        padding: EdgeInsets.all(20),
      ),
    );
  }
}
*/